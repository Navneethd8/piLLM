# AGENTS

This workspace hosts the piLLM harness — an edge-first agent for Raspberry Pi.

- `src/` — TypeScript agent loop, gateway, web API
- `pi/` — install script and systemd units
- `data/` — default SOUL/MEMORY templates copied to `~/.pillm/data/`
